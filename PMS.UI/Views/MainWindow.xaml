<Window
    x:Class="PMS.UI.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="clr-namespace:PMS.UI.Views"
    xmlns:local="clr-namespace:PMS.UI.Converters"
    Title="Project Manager Software (PMS)"
    Height="500"
    Width="800"
    MinHeight="500"
    MinWidth="800"
    WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <local:StringNotEmptyToVisibilityConverter x:Key="NonEmptyToVisibility"/>
    </Window.Resources>

    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width=".25*"/>
            <ColumnDefinition Width=".75*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" >
            <views:ProjectListView/>
        </Border>

        <Grid Grid.Column="1" Margin="5,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <views:ProjectDetailsView Grid.Row="0"/>
            <views:TaskPanelView Grid.Row="1"/>
        </Grid>
    </Grid>
</Window>