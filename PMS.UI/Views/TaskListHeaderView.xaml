<UserControl x:Class="PMS.UI.Views.TaskListHeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PMS.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Style="{StaticResource SecondRoundedBorderStyle}" Padding="4" Margin="0,5,0,0">
        <Grid Grid.Row="3" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width=".5*"/>
                <ColumnDefinition Width=".5*"/>
                <ColumnDefinition Width=".5*"/>
                <ColumnDefinition Width=".5*"/>
                <ColumnDefinition Width=".5*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Title"     FontWeight="Bold" HorizontalAlignment="Left"   Margin="10,0,10,0" Style="{StaticResource LightTextBlockStyle}"/>
            <TextBlock Grid.Column="1" Text="Deadline"    FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,10,0" Style="{StaticResource LightTextBlockStyle}"/>
            <TextBlock Grid.Column="2" Text="State"    FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,10,0" Style="{StaticResource LightTextBlockStyle}"/>
            <TextBlock Grid.Column="3" Text="Priority" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,10,0" Style="{StaticResource LightTextBlockStyle}"/>
            <TextBlock Grid.Column="4" Text="Actions"     FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,10,0" Style="{StaticResource LightTextBlockStyle}"/>

        </Grid>
    </Border>
</UserControl>
